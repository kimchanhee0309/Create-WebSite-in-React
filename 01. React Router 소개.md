# (1) React Router란?
```
React에서 URL에 따라 다른 컴포넌트(페이지)를 보여주는 라이브러리
```

# (2) React가 해결하는 문제
```
문제 (React만 쓸 때)          해결 (React Router 사용)
──────────────────────────────────────────────────────
URL이 변하지 않음          →   URL이 페이지에 맞게 변경됨
뒤로가기/앞으로가기 안됨   →   브라우저 히스토리 관리 됨
북마크 불가                →   URL로 북마크 가능
새로고침 시 초기화         →   URL 기반으로 올바른 페이지 표시
```

# (3) React Router 동작 원리
```
사용자가 /about 입력
↓
React Router가 URL 감지
↓
/about에 연결된 컴포넌트 찾기
↓
<About /> 컴포넌트를 화면에 렌더링
↓
페이지 새로고침 없이 화면 전환 완료
```

# (4) 핵심 키워드
```
BrowserRouter    - React Router의 가장 바깥 컨테이너
Routes           - Route들을 감사는 그룹
Route            - URL과 컴포넌트를 연결
Link             - 페이지 이동(a 태그 대신)
NavLink          - 현재 페이지 강조 기능이 있는 Link
useNavigate      - 버튼 클릭 등으로 페이지 이동
useParams        - URL의 동적 값 읽기(/post/:id)
Outlet           - 중첩 라우팅에서 자식 페이지 표시
```
