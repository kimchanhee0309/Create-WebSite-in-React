# 1. Link로 이동하기
* 페이지를 새로고침 없이 이동하는 방법
<details>
  <summary>(1) a 태그를 쓰면 안 되는 이유?</summary>

  * HTML의 기본 링크 태그인 `<a>`를 그냥 쓰면 SPA의 장점이 사라짐
    ```JSX
    // X
    function Home() {
      return (
        <a href="/about">소개 페이지로 이동</a>
      )
    }

    // 문제
    // - 클릭 시 브라우저가 서버에 /about을 새로 요청함
    // - 페이지 전체가 새로고침됨
    // - React 앱이 처음부터 다시 로드됨
    // -> SPA의 장점(빠름, 부드러움)이 완전히 사라짐
    ```
</details>

<details>
  <summary>(2) Link 사용법</summary>

  ```JSX
  import { Link } from 'react-route-dom'

  function Home() {
    return (
      <div>
        <h1>홈 페이지</h1>
        <Link to="/about">소개 페이지로 이동</Link>
      </div>
    )
  }
  ```
</details>

<details>
  <summary>(3) 네비게이션 바 만들기</summary>

  ```JSX
  // components/Navbar.jsx
  import { Link } from 'react-router-dom'

  function Navbar() {
    return (
      <nav>
        <Link to="/">홈</Link>
        <Link to="/about">소개</Link>
        <Link to-"/contact">연락처</Link>
      </nav>
    )
  }

  export default Navbar
  ```
  ```JSX
  // App.jsx
  import { Routes, Route } from 'react-router-dom'
  import Navbar from './components/Navbar'
  import Home from './pages/Home'
  import About from './pages/About'
  import Contact from './pages/Contact'

  function App() {
    return (
      <div>
        <Navbar />      /* 모든 페이지에서 공통으로 표시 */
        <Routes>
          <Route path="/"  element={<Home />} />
          <Route path="/about"  element={<About />} />
          <Route path="/contact"  element={<Contact />} />
        <Routes>
      </div>
    )
  }

  export default App
  ```

  * `<Navbar />`는 `<Routes>` 밖에 있기 때문에 어느 페이지로 이동해도 항상 화면에 남아 있음
</details>

<details>
  <summary>(4) Link의 추가 속성</summary>

  ```JSX
  // 스타일 적용
  <Link to="/about" style={{ color: 'blue' }}>소개</Link>

  // className 적용
  <Link to="/about" className="nav-link">소개</Link>

  // 새 탭에서 열기 (a 태그와 동일)
  <Link to="/about" target="_blank">소개</Link>
  ```
</details>

___

# 2. NavLink로 네비게이션 구현하기
<details>
  <summary>(1) NavLink란?</summary>

  * `<Link>`와 동일하게 페이지를 이동하지만, **현재 보고 있는 페이지의 링크에 자동으로 `active` 클래스를 추가해줌**
    ```
    / 페이지에 있을 때
  → "홈" 링크에만 class="active" 자동 추가

  /about 페이지에 있을 때
  → "소개" 링크에만 class="active" 자동 추가
    ```
</details>

<details>
  <summary>(2) 기본 사용법</summary>

  ```JSX
  // components/Navbar.jsx
  import { NavLink } from 'react-router-dom'

  function Navbar() {
    return (
      <nav>
        <NavLink to="/">홈</NavLink>
        <NavLink to="/about">소개</NavLink>
        <NavLink to="/contact">연락처</NavLink>
      </nav>
    )
  }

  export default Navbar
  ```
  ```CSS
  /* index.css */
  /* NavLink가 현재 페이지일 때 자동으로 .active 클래스를 붙여줌 */
  a.active {
    font-weight: bold;
    color: blue;
    text-decoration: underline;
  ```
</details>

<details>
  <summary>(3) style 함수로 동적 스타일 적용</summary>

  * CSS 파일 없이 인라인으로도 active 스타일을 줄 수 있음
  ```JSX
  <NavLink
    to="/about"
    style={({ isActive }) => ({
      color: isActive ? 'blue' : 'black',
      fontWeight: isActive ? 'bold' : 'normal'.
    })}
  >소개
  </NavLink>
  ```
</details>

<details>
  <summary>(4) className 함수로 동적 클래스 적용</summary>

  ```JSX
  <NavLink
    to="/about"
    className={({ isActive }) => isActive ? 'nav-active' : 'nav-link'}>소개</NavLink>
  ```
</details>

<details>
  <summary>(5) Link vs NavLink - 언제 무엇을?</summary>

  ```
  Link    → 단순 이동만 필요할 때 (버튼, 배너, 카드 등)
  NavLink → 네비게이션 바처럼 현재 페이지 표시가 필요할 때
  ```
</details>

___

# 3. useNavigate로 버튼 클릭 시 페이지 이동하기
* 클릭 이외의 상황(로그인 성공, 폼 제출 후 등)에서 페이지를 이동하는 방법

<details>
  <summary>(1) useNavigate가 필요한 상황</summary>

  * `<Link>`는 사용자가 직접 클릭할 때만 이동
  * But, 다음과 같은 상황에는 Link를 쓸 수 없음
  ```
  - 로그인 성공 후 → 자동으로 홈으로 이동
  - 폼 제출 완료 후 → 자동으로 완료 페이지로 이동
  - 특정 조건 충족 시 → 자동으로 다른 페이지로 이동
  - 뒤로 가기 버튼 → 이전 페이지로 이동
  ```
  * 이럴 때 코드 안에서 직접 페이지를 이동시킬 수 있는 게 `useNavigate`
</details>

<details>
  <summary>(2) 기본 사용법</summary>

  ```JSX
  import { useNavigate } from 'react-router-dom'

  function Home() {
    const navigate = useNavigate()
    const handleClick = () => {
      navigate('/about')  // /about 페이지로 이동
    }

    return (
      <div>
        <h1>홈 페이지</h1>
        <button onClick={handleClick}>소개 페이지로 이동</button>
      </div>
    )
  }
  ```
</details>

<details>
  <summary>(3) 로그인 성공 후 이동</summary>

  ```JSX
  import { useNavigate } from 'react-router-dom'

  function Login() {
    const navigate = useNavigate()
    const handleLogin = () => {
      // 로그인 처리(예: API 호출)
      const isSuccess = true // 로그인 성공 여부

      if (isSuccess) {
        navigate('/')    // 성공 시 홈으로 이동
      } else {
        alert('로그인 실패!')
      }
    }

    return (
      <div>
        <h1>로그인</h1>
        <button onClick={handleLogin}>로그인</button>
      </div>
    )
  }
  ```
</details>

<details>
  <summary>(4) 뒤로 가기 / 앞으로 가기</summary>

  ```JSX
  const navigate = useNavigate()

  // 뒤로 가기(브라우저 뒤로가기 버튼과 동일)
  navigate(-1)

  // 앞으로 가기
  navigate(1)

  // 두 페이지 뒤로
  navigate(-2)
  ```
  ```JSX
  // 실제 사용 예시
  function DetailPage() {
    const navigate = useNavigate()

    return (
      <div>
        <button onClick={() => navigate(-1)}>뒤로 가기</button>
        <h1>상세 페이지</h1>
      </div>
    )
  }
  ```
</details>

<details>
  <summary>(5) replace 옵션 - 히스토리 덮어쓰기</summary>

  ```JSX
  // 기본: 히스토리에 새 항목 추가(뒤로가기 가능)
  navigate('/home')

  // replace: 현재 히스토리를 덮어씀(뒤로가기 불가)
  navigate('/home', { replace: true })
  ```
  ```
  // replace 사용 케이스
  // 로그인 후 -> replace로 이동하면 로그인 페이지로 뒤로가기 불가
  navigate('/home', { replace: true })
  ```
</details>

<details>
  <summary>(6) Link vs NavLink vs useNavigate 정리</summary>

  ```txt
  Link        : 클릭해서 이동(일반 링크)
  NavLink     : 클릭해서 이동 + 현재 페이지 스타일 표시(네비게이션)
  useNavigate : 코드로 자동 이동(로그인 후, 폼 제출 후 등)
  ```
</details>
