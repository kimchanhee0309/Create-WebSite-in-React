# 1. Link로 이동하기
* 페이지를 새로고침 없이 이동하는 방법
<details>
  <summary>(1) a 태그를 쓰면 안 되는 이유?</summary>

  * HTML의 기본 링크 태그인 `<a>`를 그냥 쓰면 SPA의 장점이 사라짐
    ```JSX
    // X
    function Home() {
      return (
        <a href="/about">소개 페이지로 이동</a>
      )
    }

    // 문제
    // - 클릭 시 브라우저가 서버에 /about을 새로 요청함
    // - 페이지 전체가 새로고침됨
    // - React 앱이 처음부터 다시 로드됨
    // -> SPA의 장점(빠름, 부드러움)이 완전히 사라짐
    ```
</details>

<details>
  <summary>(2) Link 사용법</summary>

  ```JSX
  import { Link } from 'react-route-dom'

  function Home() {
    return (
      <div>
        <h1>홈 페이지</h1>
        <Link to="/about">소개 페이지로 이동</Link>
      </div>
    )
  }
  ```
</details>

<details>
  <summary>(3) 네비게이션 바 만들기</summary>

  ```JSX
  // components/Navbar.jsx
  import { Link } from 'react-router-dom'

  function Navbar() {
    return (
      <nav>
        <Link to="/">홈</Link>
        <Link to="/about">소개</Link>
        <Link to-"/contact">연락처</Link>
      </nav>
    )
  }

  export default Navbar
  ```
  ```JSX
  // App.jsx
  import { Routes, Route } from 'react-router-dom'
  import Navbar from './components/Navbar'
  import Home from './pages/Home'
  import About from './pages/About'
  import Contact from './pages/Contact'

  function App() {
    return (
      <div>
        <Navbar />      /* 모든 페이지에서 공통으로 표시 */
        <Routes>
          <Route path="/"  element={<Home />} />
          <Route path="/about"  element={<About />} />
          <Route path="/contact"  element={<Contact />} />
        <Routes>
      </div>
    )
  }

  export default App
  ```

  * `<Navbar />`는 `<Routes>` 밖에 있기 때문에 어느 페이지로 이동해도 항상 화면에 남아 있음
</details>

<details>
  <summary>(4) Link의 추가 속성</summary>

  ```JSX
  // 스타일 적용
  <Link to="/about" style={{ color: 'blue' }}>소개</Link>

  // className 적용
  <Link to="/about" className="nav-link">소개</Link>

  // 새 탭에서 열기 (a 태그와 동일)
  <Link to="/about" target="_blank">소개</Link>
  ```
</details>

___

# 2. NavLink로 네비게이션 구현하기
<details>
  <summary>(1) NavLink란?</summary>
</details>

<details>
  <summary>(2) 기본 사용법</summary>
</details>

<details>
  <summary>(3) style 함수로 동적 스타일 적용</summary>
</details>

<details>
  <summary>(4) className 함수로 동적 클래스 적용</summary>
</details>

<details>
  <summary>(5) Link vs NavLink - 언제 무엇을?</summary>
</details>

___

# 3. useNavigate로 버튼 클릭 시 페이지 이동하기
<details>
  <summary>(1) useNavigate가 필요한 상황</summary>
</details>

<details>
  <summary>(2) 기본 사용법</summary>
</details>

<details>
  <summary>(3) 로그인 성공 후 이동</summary>
</details>

<details>
  <summary>(4) 뒤로 가기 / 앞으로 가기</summary>
</details>

<details>
  <summary>(5) replace 옵션 - 히스토리 덮어쓰기</summary>
</details>

<details>
  <summary>(6) Link vs NavLink vs useNavigate 정리</summary>
</details>
